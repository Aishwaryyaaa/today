- name: find
  hosts: all
  become: true
  tasks:
  - name: to find
    find: 
      paths: /root/test/test1
      file_type: directory
      patterns: '*'
      register: ash
  - name:
    debug:
      msg: "{{ash}}"